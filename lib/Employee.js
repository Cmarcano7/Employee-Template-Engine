// TODO: Write code to define and export the Employee class
function Employee (name, position, id, email) {
    this.name = name;
    this.position = position;
    this.id = id;
    this.email = email;
};


module.export(employee);